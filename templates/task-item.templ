package templates

import (
	"fmt"
	"sqlite/model"
)

templ TaskItem(task model.Task, isNew bool) {
	<a href={ templ.URL(fmt.Sprintf("/tasks/%d", task.ID)) } class={ "task-item", templ.KV("new", isNew) } data-id={ fmt.Sprint(task.ID) }>
		{ task.Title }
	</a>
}
